// import { useParams } from "react-router-dom";

export const Report = () => {
    // const { id } = useParams<{ id: string }>();

    return (
        <div>
            <h1>This is my report</h1>
        </div>
    );
};
